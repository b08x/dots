exec_always {
  'mkdir -p $HOME/.config/waybar && ln -sf $HOME/.config/sway/theme.css $HOME/.config/waybar/'
  'mkdir -p $HOME/.config/wlogout && ln -sf $HOME/.config/sway/theme.css $HOME/.config/wlogout/'
}
